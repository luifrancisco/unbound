apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unbound-cache-pvc
  namespace: unbound
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: local-path
  volumeMode: Filesystem
